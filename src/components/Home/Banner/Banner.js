import React from 'react';
import useStyles from './BannerStyle';

const Banner = () => {
    const { root } = useStyles();
    return (
        <div className={root}>

        </div>
    );
};

export default Banner;